---
description: Turn OFF Make Text Great Again — restore previous style and spinner phrases
allowed-tools: Bash(bash:*)
---

MTGA toggle result:

!`bash "${CLAUDE_PLUGIN_ROOT}/scripts/mtga.sh" off`

Relay the result above to the user in their language. Mention that the restored settings take effect after `/clear` or a session restart.
